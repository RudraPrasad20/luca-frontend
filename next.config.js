// /** @type {import('next').NextConfig} */
// next.config.js
const nextConfig = {
  images: {
    domains: ["assets.example.com", "assets-global.website-files.com"],
    // Add your other image domains or patterns here if needed
    // remotePatterns: [], // Uncomment this line if you have remote patterns
  },
};

module.exports = nextConfig;
